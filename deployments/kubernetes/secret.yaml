# TEMPLATE ONLY — do not commit real credentials.
# Create this Secret manually or via your secrets manager (Vault, Sealed Secrets, ESO, etc.)
#
# Example:
#   kubectl create secret generic aether-secrets \
#     --namespace aether \
#     --from-literal=DATABASE_URL='postgres://aether:password@postgres:5432/aether?sslmode=require' \
#     --from-literal=JWT_SECRET='<random-32+-char-string>'
apiVersion: v1
kind: Secret
metadata:
  name: aether-secrets
  namespace: aether
  labels:
    app.kubernetes.io/name: aether
    app.kubernetes.io/component: server
type: Opaque
stringData:
  DATABASE_URL: "postgres://aether:CHANGE_ME@postgres:5432/aether?sslmode=require"
  JWT_SECRET: "CHANGE_ME_to_a_random_32_plus_character_string"
