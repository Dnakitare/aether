# Enable Automatic Reflection

Enable automatic reflection at the end of each session.

## Action

Write "on" to `.claude/.reflect-auto` to enable auto-reflection.

```bash
echo "on" > .claude/.reflect-auto
```

## Confirmation

Respond: "Auto-reflect is now **enabled**. I'll prompt for reflection at the end of each session to capture learnings and update skill files."
